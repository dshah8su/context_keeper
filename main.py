from rich.console import Console

console = Console()

if __name__ == "__main__":
    console.print("[bold cyan]context_keeper[/bold cyan] — starting up")
    console.print("Run [bold]streamlit run ui/app.py[/bold] to launch the web UI")
